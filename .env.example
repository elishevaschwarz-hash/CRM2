# ─── Backend Environment Variables ──────────────────────────────────────────
# Copy this to .env and fill in your actual values

# Supabase Database
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key-here

# Backend CORS
# In production (Railway), set this to your frontend service URL
# Example: https://crm1-frontend.up.railway.app
CORS_ORIGIN=*

# Flask Environment
# Set to "development" for local dev, "production" for Railway
FLASK_ENV=production

# Port (auto-set by Railway, but needed for local dev)
PORT=5001

# ─── Frontend Environment Variables ──────────────────────────────────────────
# These are set at container startup by entrypoint.sh

# Backend API URL
# In production (Railway), set this to your backend service URL
# Example: https://crm1-backend.up.railway.app
API_URL=http://localhost:5001

# Port (auto-set by Railway)
PORT=3000
